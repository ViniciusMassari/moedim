/**
 * O hook useColorScheme retorna o esquema de cores atual do dispositivo da preferência do usuário (claro ou escuro).
 * @returns 'light' | 'dark' | null'
 */
export { useColorScheme } from 'react-native';
